apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aipass-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: aipass.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: aipass-backend-service
                port:
                  number: 80
