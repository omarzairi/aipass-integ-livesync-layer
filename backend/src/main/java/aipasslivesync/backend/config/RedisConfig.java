package aipasslivesync.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {
    // Spring Boot auto-configures StringRedisTemplate and RedisConnectionFactory.
    // This class exists as a placeholder for future custom Redis configuration.
}
